# JSON-parser
Normal JSON-parser that intakes a json and digests it.

## Usage
```bash
python3 parser.py <file-path.json> # this command parses the given json
python3 run_tests.py               # this command runs all the unit tests from tests-folder
```
